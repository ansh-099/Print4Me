<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tracking.team.print4me">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoprint4me"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InitialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UploadPDF" />
        <activity android:name=".MoreDetails" />
        <activity android:name=".Pay" />
        <activity android:name=".Location" />
        <activity android:name=".TotalPrice" />
        <activity android:name=".PageNumber" />
        <activity android:name=".Banding" />
        <activity android:name=".PaperGSM" />
        <activity android:name=".PaperSize" />
        <activity android:name=".PaperType" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>